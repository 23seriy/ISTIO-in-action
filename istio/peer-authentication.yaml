# Strict mTLS: All service-to-service communication must be encrypted
# Use case: Zero-trust networking — no plaintext traffic allowed in the mesh
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: strict-mtls
  namespace: istio-demo
spec:
  mtls:
    mode: STRICT
