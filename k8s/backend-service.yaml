apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: istio-demo
  labels:
    app: backend
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: backend
