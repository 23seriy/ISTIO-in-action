apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: istio-demo
  labels:
    app: frontend
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: frontend
